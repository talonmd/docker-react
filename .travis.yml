language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t talonmd/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true talonmd/docker-react npm run test
